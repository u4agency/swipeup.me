{% if widget.widget == 'text' %}
    {{ include('_components/create/widgets/text.html.twig', {widgetData: widget.widgetData, widgetType}) }}
{% elseif widget.widget == 'button' %}
    {{ include('_components/create/widgets/button.html.twig', {widgetData: widget.widgetData, widgetType}) }}
{% elseif widget.widget == 'email' %}
    {{ include('_components/create/widgets/email.html.twig', {widgetData: widget.widgetData, widgetType}) }}
{% elseif widget.widget == 'newsletter' %}
    {{ include('_components/create/widgets/newsletter.html.twig', {widgetData: widget.widgetData, widgetType, widgetSwipe: widget}) }}
{% endif %}