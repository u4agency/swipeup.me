<a
        class="
            w-full py-2 px-6 text-white font-medium text-xl rounded-2xl flex gap-3 items-baseline justify-center
            md:w-auto
            {{ color }}
        "
        href="{{ link }}"
        target="_blank"
>
    <span class="self-center fill-white">{{ icon|raw }}</span>
    <span>{{ text }}</span>
</a>
